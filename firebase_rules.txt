{
  "rules": {
    "rooms": {
      "$roomId": {
        // Izinkan baca untuk siapa saja yang tahu roomId (PIN)
        ".read": "true",
        // Izinkan tulis jika user sudah login (Anonymous/Google)
        ".write": "auth != null",
        
        "players": {
          "$playerId": {
            // Hanya user bersangkutan yang bisa edit datanya sendiri
            ".write": "auth != null && auth.uid == $playerId"
          }
        },
        "votes": {
          "$playerId": {
            ".write": "auth != null && auth.uid == $playerId"
          }
        },
        "dice": {
          "$playerId": {
            ".write": "auth != null && auth.uid == $playerId"
          }
        }
      }
    }
  }
}